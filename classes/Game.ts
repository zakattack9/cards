class Game {
  gameOver: boolean;
  winner: Player;

  constructor() {
    this.gameOver = false;
    this.winner = null;
  }

  validPlay(card: Card[]) {
    // checks if cards played is a valid move
  }
}